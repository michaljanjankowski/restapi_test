[project]
name = "restapi-test"
version = "0.1.0"
description = "Tests for the cat facts REST API"
requires-python = ">=3.10"
dependencies = [
    "requests==2.34.2",
]

[dependency-groups]
dev = [
    "pytest==9.1.1",
]

[tool.uv]
package = false
